# Requisitos de TecNet Finder

# Se requiere tener instalado Python 3.10 o superior.
# Las librerías re, math, tkinter, ipaddress y collections pertenecen a la biblioteca estándar de Python.

openpyxl
Pillow

# Instalación:
# 1. Abrir una terminal en la carpeta del proyecto.
# 2. Ejecutar el siguiente comando:
# py -m pip install -r requirements.txt

# Si el comando anterior no funciona, usar:
# python -m pip install -r requirements.txt
